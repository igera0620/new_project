<div class="relative isolate overflow-hidden bg-gray-900">
  <div class="absolute inset-0 -z-10 h-full w-full stroke-white/10 [mask-image:radial-gradient(100%_100%_at_top_right,white,transparent)]" aria-hidden="true">
    <svg>
      <defs>
        <pattern id="pattern" width="200" height="200" x="50%" y="-1" patternUnits="userSpaceOnUse">
          <path d="M.5 200V.5H200" fill="none" />
        </pattern>
      </defs>
      <rect width="100%" height="100%" stroke-width="0" fill="url(#pattern)" />
    </svg>
  </div>

  <div class="mx-auto max-w-7xl px-6 pb-24 pt-16 sm:pb-32 lg:flex lg:px-8 lg:py-40">
    <div class="mx-auto max-w-2xl flex-shrink-0 lg:mx-0 lg:max-w-xl lg:pt-8">
      <h1 class="text-4xl font-bold tracking-tight text-white sm:text-5xl">お問い合わせ</h1>
      <p class="mt-6 text-lg text-gray-300">
        ご質問やご要望がございましたら、以下のフォームよりお問い合わせください。
      </p>

      <%= form_with model: @contact, url: confirm_path, local: true, data: { turbo: false }, class: "mt-10 space-y-6" do |f| %>
        <div>
          <%= f.label :name, 'お名前', class: 'block text-sm font-medium text-white' %>
          <div class="mt-1">
            <%= f.text_field :name, required: true, class: 'block w-full rounded-md border border-gray-300 px-3 py-2 text-white bg-gray-800 sm:text-sm' %>
          </div>
        </div>

        <div>
          <%= f.label :email, 'メールアドレス', class: 'block text-sm font-medium text-white' %>
          <div class="mt-1">
            <%= f.email_field :email, required: true, class: 'block w-full rounded-md border border-gray-300 px-3 py-2 text-white bg-gray-800 sm:text-sm' %>
          </div>
        </div>

        <div>
          <%= f.label :subject, 'お問い合わせの種類', class: 'block text-sm font-medium text-white' %>
          <div class="mt-1">
            <%= f.select :subject, [
                ['一般のお問い合わせ', 'general'],
                ['サポート', 'support'],
                ['フィードバック', 'feedback'],
                ['その他', 'other']
              ], {}, class: "block w-full rounded-md border border-gray-300 px-3 py-2 text-white bg-gray-800 sm:text-sm" %>
          </div>
        </div>

        <div>
          <%= f.label :message, 'お問い合わせ内容', class: 'block text-sm font-medium text-white' %>
          <div class="mt-1">
            <%= f.text_area :message, required: true, rows: 5, class: 'block w-full rounded-md border border-gray-300 px-3 py-2 text-white bg-gray-800 sm:text-sm' %>
          </div>
        </div>

        <div>
          <%= f.submit '入力内容を確認する', class: 'flex w-full justify-center rounded-md bg-indigo-600 py-2 px-4 text-sm font-medium text-white shadow-sm hover:bg-indigo-500 focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2' %>
        </div>
      <% end %>
    </div>
  </div>
</div>
